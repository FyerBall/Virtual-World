// TODO show false, modalContent ""
// ? if post sent then show true,  modalContent "sent"
// * useEffect to show it for x sec
import React from 'react';

function Modal({ modalContent }) {
  return (
    <div>
      <p>modalContent</p>
    </div>
  );
}

export default Modal;
